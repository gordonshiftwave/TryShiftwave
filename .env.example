# Optional. Leave unset to load /locations.json from this app.
# Point at a published Google Sheet CSV (File → Share → Publish to web → CSV)
# or any JSON/CSV URL that matches the location schema.
# VITE_LOCATIONS_URL=https://docs.google.com/spreadsheets/d/e/SHEET_ID/pub?output=csv
