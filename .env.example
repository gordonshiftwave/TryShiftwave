# Optional. Publish the qualified locations Google Sheet as CSV and paste the URL.
# File → Share → Publish to web → Comma-separated values (.csv)
# VITE_LOCATIONS_CSV_URL=https://docs.google.com/spreadsheets/d/e/XXXX/pub?output=csv
